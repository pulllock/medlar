spring:
  application:
    name: spring-boot-websocket-netty-cluster-redis
  data:
    redis:
      database: 0
      host: localhost
      port: 6379
      # password:
      timeout: 2000
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1