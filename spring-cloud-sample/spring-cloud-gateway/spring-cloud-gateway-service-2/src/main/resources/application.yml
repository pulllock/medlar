server:
  port: 8082

spring:
  application:
    name: spring-cloud-gateway-service-2

eureka:
  instance:
    instance-id: spring-cloud-gateway-service-2
    hostname: spring-cloud-gateway-service-2
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
